# Terraform state files and backups
**/*.tfstate
**/*.tfstate.*
*.tfstate.backup

# Terraform .cache directory
.terraform/

# Crash log files
crash.log

# Exclude all .tfvars files, which may contain sensitive data
# except for an example file
#
# *.tfvars
# !*.tfvars.example

# Module and provider information (might be in .terraform)
.terraform.lock.hcl

# config for vscode plugins
.vscode/

# some html files are generated from templates
app/index.html
app/live.html
app/history.html
app/bulk.html
app-test/index.html
app-test/live.html
app-test/history.html
app-test/bulk.html

# any csv or json files in scripts folder are temporary
scripts/*.csv
scripts/*.json


